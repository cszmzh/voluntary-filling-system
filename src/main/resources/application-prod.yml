spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/volunteer_new?characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    password: test123456

server:
  port: 8080

logging:
  file:
    name: logback-spring.xml